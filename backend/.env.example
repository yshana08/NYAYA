# Optional. If set, case classification calls the real Claude API
# (Claude Haiku 4.5) instead of the built-in offline keyword matcher.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

PORT=4000
